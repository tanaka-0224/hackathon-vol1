import { Link} from 'react-router-dom';
import "../types/Visual.css";

const Rules = () => {
    return (
      <div>
        <h1>rules</h1>
        <Link to="/">
            <button>Back</button>
        </Link>
      </div>
    );
  };
  
  export default Rules;